---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No top-level namespace — see kubernetes/main/apps/storage/kustomization.yaml
# for the rationale (it overrides install.yaml flux-system namespaces).
resources:
  # Pre Flux-Kustomizations
  - namespace.yaml
  # Flux-Kustomizations
  - rook-ceph-operator/install.yaml
  - rook-ceph-cluster/install.yaml
