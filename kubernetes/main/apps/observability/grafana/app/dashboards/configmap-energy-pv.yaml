# This file has been removed.
# The grafana-dashboard-energy-pv ConfigMap is now generated by the
# configMapGenerator in ../kustomization.yaml, reading dashboards/energy-pv.json
# directly.  Edit energy-pv.json — it is the single source of truth.
